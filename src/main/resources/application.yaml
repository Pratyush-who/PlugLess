spring:
  application:
    name: PlugLess
  profiles:
    active: local
  data:
    mongodb:
      auto-index-creation: true

security:
  jwt:
    expiration-ms: 86400000

logging:
  level:
    root: INFO
    com.example.PlugLess: DEBUG
